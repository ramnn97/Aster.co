import { createAsterConfig } from "Aster-agent-playwright-config/config";

export default createAsterConfig({
  // Add your custom playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:3000',
  // },
});
